IF {pio_crtrec}.{columnPKName} IS NULL THEN
    SELECT NVL(MAX({columnPKName}), 0) + 1
    INTO {pio_crtrec}.{columnPKName}
    FROM {tableName}
    WHERE (1=1)
    {-TABLE_DEP_JOIN_PARENT-};
END IF;
