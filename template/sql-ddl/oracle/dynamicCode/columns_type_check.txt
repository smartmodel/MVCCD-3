IF {pio_crtrec}.{columnName} IS NOT NULL THEN
    {packageResourcesName}.{procedureResourcesCheckDatatypeWord} (
    '{tableName}',
    '{columnName}',
    {pio_crtrec}.{columnName},
    '{noError}',
    '{messError}' ) ;
 END IF ;
