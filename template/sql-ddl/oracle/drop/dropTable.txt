DECLARE nb_table PLS_INTEGER;

BEGIN
    SELECT count(*) INTO nb_table from user_tables WHERE table_name = UPPER('{tableName}');
    IF nb_table = 1 then
        EXECUTE IMMEDIATE 'DROP table {tableName} CASCADE CONSTRAINT PURGE';
    END IF;
END;
