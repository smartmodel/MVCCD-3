CREATE OR REPLACE PACKAGE {packageName} IS
    PROCEDURE {autogen_columns}({pio_crtrec} IN OUT {tableName}%ROWTYPE);
    PROCEDURE {autogen_columns_ins}({pio_crtrec} IN OUT {tableName}%ROWTYPE);
    PROCEDURE {autogen_columns_upd}({pio_crtrec} IN OUT {tableName}%ROWTYPE);
    PROCEDURE {uppercase_columns}({pio_crtrec} IN OUT {tableName}%ROWTYPE);
    PROCEDURE {checktype_columns}({pio_crtrec} IN OUT {tableName}%ROWTYPE);
    PROCEDURE {column_PEA}({pio_crtrec} IN OUT {tableName}%ROWTYPE);
    PROCEDURE {frozen_columns}( {pio_newrec} IN OUT {tableName}%ROWTYPE
        , {pio_oldrec} IN OUT {tableName}%ROWTYPE);
    PROCEDURE {tree_or_list_loop};
    PROCEDURE {tree_or_list_onlyone};
    --{-INS_JN_SPEC-}

    TYPE {type_}{tableName}  IS TABLE OF {tableName}%ROWTYPE INDEX BY PLS_INTEGER;
    {vg_}{tableName} {type_}{tableName};

    {vg_insteadof_call} BOOLEAN := FALSE;
END;