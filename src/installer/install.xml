<izpack:installation version="5.0"
                     xmlns:izpack="http://izpack.org/schema/installation"
                     xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                     xsi:schemaLocation="http://izpack.org/schema/installation http://izpack.org/schema/5.0/izpack-installation-5.0.xsd">
    <!-- Informations about the software -->
    <info>
        <appname>ArcDataModeler</appname>
        <appversion>1.0</appversion>
        <authors>
            <author name="Fabrice Camus" email="fabrice.camus@he-arc.ch"/>
            <author name="Melvyn Vogelsang" email="melvyn.vogelsang@he-arc.ch"/>
        </authors>
    </info>

    <!-- Define the language of the installer -->
    <locale>
        <langpack iso3="fra"/>
    </locale>

    <!-- GUI configuration -->
    <guiprefs width="800" height="600" resizable="no"/>

    <!-- The resources section. The ids must be there ones if you want to use the LicencePanel and/or the InfoPanel. -->
    <resources>
        <res src="C:/Users/marro/Bachelor/ArcDataModeler/src/installer/shortcutSpec.xml" id="shortcutSpec.xml"/>
    </resources>

    <!-- The native library -->
    <natives>
        <native type="izpack" name="ShellLink.dll"/>
        <native type="izpack" name="ShellLink_x64.dll"/>
    </natives>

    <!-- The panels section. We indicate here wich panels we want to use. The order will be respected. -->
    <panels>
        <panel classname="HelloPanel" id="hellopanel"/>
        <panel classname="TargetPanel" id="targetpanel"/>
        <panel classname="PacksPanel" id="packspanel"/>
        <panel classname="InstallPanel" id="installpanel"/>
        <panel classname="ShortcutPanel" id="shortcutpanel"/>
        <panel classname="FinishPanel" id="finishpanel"/>
    </panels>

    <!-- Packs represents the modules created during the installation -->
    <packs>
        <!-- Core files (jar de l'application) -->
        <pack name="Core files" required="yes" hidden="false">
            <description>Application base files</description>
            <file src="C:/Users/marro/Bachelor/ArcDataModeler/src/installer/icon" targetdir="${INSTALL_PATH}"/>
            <file src="C:/Users/marro/Bachelor/ArcDataModeler/out/artifacts/MVCCD_3_jar/MVCCD-3.jar"
                        targetdir="${INSTALL_PATH}"/>
        </pack>
    </packs>
</izpack:installation>